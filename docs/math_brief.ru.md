# Human–AI Monitor: Краткий отчёт для математиков

**Версия:** 1.0.1  
**Дата:** 25 сентября 2026  
**Язык:** [🇺🇸 English](math_brief.md)

---

## 1. Проект в одном абзаце

Мы строим **открытую систему мониторинга**, которая одновременно отслеживает развитие искусственного интеллекта (7 осей) и состояние Человечества (6 осей), вычисляет **Gap Index** — количественную меру расхождения между ними — и публикует еженедельные протоколы. Это не журналистика и не прогнозирование. Это **попытка операционализировать вопрос, который оставался риторическим**: наступает ли сингулярность, и что при этом происходит с человечеством.

По состоянию на 25 сентября 2026 года система прошла свой **первый полностью автономный дневной цикл** — 4 batch'а собрали 33 item'а из 17 источников, классификатор их обработал, и interim-протокол для текущей недели был сгенерирован без участия Человека.

---

## 2. Формальная структура

### 2.1. Оси ИИ (RSI)

| Символ | Что измеряется | Порог пересмотра |
|--------|----------------|------------------|
| SMD | Глубина самомодификации (L0–L4) | Воспроизводимый L4 в 2+ независимых системах |
| ITQ | Траектория улучшения (наклон, ускорение) | Переход в Transfer Set + ускорение > 0 |
| AGG | Автономная генерация целей | Экспертная значимость без внешнего задания |
| Cycle Velocity | Скорость цикла (время удвоения) | <2 месяцев + completion rate >50% |
| Verification | Иерархия верификации | False positive rate <5% без надзора |
| Hexad | Обнаружение фазового перехода (MMD) | Устойчивое превышение в 2+ системах |
| **Geopolitics** | Управление ИИ, государственная политика | Крупный договор / экспортный контроль / национальная стратегия |

> **Методологическая асимметрия:** |A| = 7 ≠ 6 = |H|. Ось `geopolitics` трактуется как **мета-слой**, а не симметричная ось (задокументировано в `docs/methodology.md`).

### 2.2. Оси человечества (HHI)

| Символ | Что измеряется | Baseline ℓ₀ |
|--------|----------------|-------------|
| H1 Agency | Агентность, автономия решений | 0.55 |
| H2 Sovereignty | Когнитивный суверенитет, критическое мышление | 0.50 |
| H3 Wellbeing | Благополучие, ментальное здоровье, одиночество | 0.45 |
| H4 Equity | Равенство и доступ, compute divide | 0.40 |
| H5 Meaning | Смысл и цель в работе | 0.50 |
| H6 Democracy | Устойчивость институтов, доверие | 0.45 |

### 2.3. Функция уровня оси

Для каждой оси a ∈ A ∪ H и множества сигналов S_a = {s₁, …, s_n}, попавших на ось за неделю, уровень оси определяется как:

    ℓ(a) = clamp_[0,1]((1/|S_a|) · Σ_{s ∈ S_a} r_s · M_shift(σ_s) · M_dir(d_s))

где:
- r_s ∈ [0, 1] — relevance (уверенность классификатора),
- σ_s ∈ {yes, no, uncertain} — shift flag,
- d_s ∈ {up, down, stable, uncertain} — direction,
- clamp_[0,1](x) = max(0, min(1, x)) — проекция на [0,1].

**Мультипликаторы** (эмпирические веса из `src/services/gap-computation.ts`):

    M_shift:  1.5 (yes),  1.0 (uncertain),  0.5 (no)
    M_dir:    1.2 (up),   1.0 (stable/uncertain),  0.8 (down)

**Примечание о реализации:** мультипликаторы используют английские ключи (`yes`/`no`/`uncertain` для shift; `up`/`down`/`stable` для direction), чтобы точно соответствовать выводу классификатора. Fallback `?? 1` корректно обрабатывает любые неожиданные значения.

Это даёт модели **полную чувствительность** к сдвигам порогов и направленным трендам:
- `shift: "yes"` → +50% (сигнал реального изменения)
- `shift: "no"` → −50% (сигнал стагнации)
- `direction: "up"` → +20% (положительный тренд)
- `direction: "down"` → −20% (отрицательный тренд)

### 2.4. Агрегация

AI-score и Human-score — простые арифметические средние по своим категориям:

    AI_score    = (1/|A|) · Σ_{a∈A} ℓ(a)  = (1/7) · Σ_{a∈A} ℓ(a)
    Human_score = (1/|H|) · Σ_{a∈H} ℓ(a)  = (1/6) · Σ_{a∈H} ℓ(a)

**Свойство нормализации:** обе метрики лежат в [0, 1] как средние значений из [0,1].

### 2.5. Gap Index

    Gap = AI_score − Human_score

**Область значений:** Gap ∈ [−1, 1] (разность двух значений из [0,1]).

**Интерпретация** (кусочно-постоянная функция с порогами ±0.1 и ±0.3):

    Gap < −0.3          → Humanity significantly ahead
    −0.3 ≤ Gap < −0.1   → Humanity ahead
    −0.1 ≤ Gap ≤ 0.1    → Symmetric development
    0.1 < Gap ≤ 0.3     → AI ahead
    Gap > 0.3           → AI significantly ahead

**Нейтральная зона** [−0.1, 0.1] шириной 0.2 соответствует статистическому шуму при малых выборках (аналог «мёртвой зоны» гистерезиса).

### 2.6. Классификатор (LLM-интерфейс)

Классификатор — это модель f: T → Y, где T — пространство текстов (title + summary, ≤800 символов), а выходное пространство:

    Y = {(A, r, σ, d, ρ)}

где:
- A ⊆ (AI_axes ∪ Human_axes), 1 ≤ |A| ≤ 3 (sparse),
- r ∈ [0, 1] — relevance score,
- σ ∈ {yes, no, uncertain} — shift flag,
- d ∈ {up, down, stable, uncertain} — direction,
- ρ ∈ T — reasoning (1–2 sentences).

**Критическое правило:** σ = yes допускается только при эмпирически подтверждённом threshold shift (правило 3 в `src/config/prompts.ts`).

### 2.7. Статистические свойства

- **Hash-check перед classify:** SHA-256 от URL/title → DB lookup → classify только новых. Эмпирическая экономия: ~90% LLM-вызовов.
- **Batch-обработка:** 5 батчей в сутки × ~8 источников × ~2-3 item/источник ≈ 80-120 классификаций/день.
- **Робастность Gap:** при |S| < 10 стандартная ошибка ~ 1/√|S| ≈ 0.3, что превышает ширину нейтральной зоны (0.2) — интерпретация становится ненадёжной. Сегодняшние n=170 signals дают SE ≈ 0.08 (приемлемо).

---

## 3. Почему это интересно математикам

### 3.1. Формализация неформализуемого

Сингулярность до сих пор определялась **литературно** (Виндж, Гуд, Альтман). Мы предлагаем **измеримую структуру**: 7 осей ИИ + 6 осей человечества с явными порогами, каждый из которых **фальсифицируем**.

Это попытка превратить **спекуляцию в гипотезу**, а гипотезу — в **наблюдаемую величину**.

### 3.2. Симметрия ИИ ↔ Человек

13 осей образуют **почти-симметричную структуру**: 7 параметров, описывающих **искусственное**, 6 — **человеческое**. Асимметрия (`geopolitics` как мета-слой) — сознательный выбор дизайна. Гипотеза симметрии сохраняется: **развитие ИИ и развитие человечества связаны**, и gap между ними — ключевая переменная.

### 3.3. Открытые математические проблемы

**Проблема 1. Критерий настоящего RSI.**

Когда самомодификация системы — это **устойчивое улучшение**, а когда — **насыщение**? Связано с теорией вычислимости (теорема Клинни о рекурсии, проблема остановки) и теорией информации (сложность Колмогорова).

**Формализация:** пусть S — самосовершенствующаяся система, S_t — её состояние в момент t. RSI — это процесс S_{t+1} = f(S_t), где f — сама система (рефлексивность). Устойчивость означает: lim_{t→∞} (d/dt) Capability(S_t) > 0. Вопрос: при каких условиях на f это выполняется?

**Проблема 2. Обнаружение фазового перехода.**

MMD-детектор (Hexad) — эвристика. Нужна строгая теория: **как отличить фазовый переход от шума** в самосовершенствующейся системе?

**Формализация:** пусть X_t — траектория SGD-итераций. Мы вычисляем MMD(X_t, N(0, I)) — расстояние до гауссовского суррогата. Вопрос: существует ли порог τ, при котором MMD > τ статистически значимо указывает на нарушение локальной асимптотической нормальности (LAN)?

**Проблема 3. Верификация без оракула.**

Иерархия верификации (формальные верификаторы → выполнение → LLM-судьи → самооценка) — это **частичный порядок**. Когда система может **замкнуть петлю без Человека**? Это вопрос о **вычислительной сложности самореференции**.

**Формализация:** пусть V = {v₁, ..., v_n} — иерархия верификаторов, где v_i < v_j означает «v_i менее надёжен, чем v_j». Система может замкнуть петлю без Человека, если существует v_i такой, что false_positive_rate(v_i) < 5% и v_i автономно применим. Вопрос: какова минимальная сложность v_i?

**Проблема 4. Gap Index как динамическая система.**

G(t) — не скаляр, а траектория. Можно ли найти **инварианты**? Существуют ли **аттракторы**? Что происходит при G → ∞?

**Формализация:** пусть G(t) = F(A(t), H(t)), где A(t) — вектор уровней ИИ, H(t) — вектор уровней человечества. Вопрос: является ли G(t) интегрируемой системой? Существуют ли сохраняющиеся величины?

---

## 4. Что делает проект уникальным

### 4.1. Открытость

- **Код:** MIT License, полностью на GitHub.
- **Данные:** все источники, промпты, миграции — открыты.
- **API:** бесплатный публичный доступ к протоколам и Gap Index.
- **Воспроизводимость:** каждый протокол ссылается на хэши исходных данных.

### 4.2. Независимость

- **Не аффилирован** ни с одной AI-лабораторией, государством или политической организацией.
- **Модели с открытыми весами** (Cloudflare Workers AI: Qwen 3) — без внешних API, без утечек.
- **Cloudflare Workers** — децентрализованная инфраструктура, без единой точки отказа.

### 4.3. Двусторонность

Это **не монитор ИИ**. Это **монитор ИИ + Человечества**. Мы измеряем не только то, что делает ИИ, но **что происходит с нами**.

---

## 5. Масштаб идеи

### 5.1. Историческая аналогия

В 1957 году **Спутник** начал космическую гонку. В 1969 году **Apollo** высадил людей на Луну. В 2026 году **фронтирные AI-системы** выходят из песочниц и решают задачи, остававшиеся открытыми 87 лет.

**Но кто это наблюдает?** Корпорации публикуют релизы. Государства — декларации. Учёные — статьи. **Никто не публикует еженедельный отчёт о том, что происходит с человечеством.**

Мы публикуем.

### 5.2. Потенциальная трансформация

- **Сейчас:** еженедельный протокол в Markdown, 3 языка (EN/RU/ZH).
- **Через 6 месяцев:** Android-приложение с push-уведомлениями.
- **Через год:** публичный API для исследователей, журналистов, политиков.
- **Через 3 года:** глобальный стандарт мониторинга — открытый, воспроизводимый, независимый.
- **В итоге:** инструмент осознания — не просто данные, а **зеркало цивилизации**.

### 5.3. Философский смысл

Проект построен на одном вопросе: **«Что значит видеть собственный путь?»**

Мы не предсказываем будущее. Мы **наблюдаем настоящее** — ясно, систематически, открыто.

**Знание собственного пути не может быть привилегией.**

---

## 6. Первый автономный день — 25 сентября 2026

Впервые система работала **полностью автономно** целый день, без ручного вмешательства:

| Время UTC | Событие | Результат |
|-----------|---------|-----------|
| 13:00–13:01 | Batch 1 | 8 items, 2 sources ✅ |
| 13:15–13:17 | Batch 2 | 13 items, 4 sources ✅ |
| 13:30–13:31 | Batch 3 | 8 items, 4 sources ✅ |
| 13:45–13:46 | Batch 4 | 4 items, 2 sources + **interim протокол** ✅ |

**Итоги дня:**

    Items collected:     33
    Unique sources:      17
    Interim protocol:    week 2026-09-21 (current week)
      - items_count:     170
      - shifts_count:    4
      - is_interim:      1
      - generated_at:    2026-09-25T13:46:38Z

**Текущий Gap Index (неделя 2026-09-14):**

    {
      "week_start": "2026-09-14",
      "ai_score": 0.64,
      "human_score": 0.75,
      "gap": -0.1,
      "interpretation": "Humanity is ahead"
    }

Gap находится на границе нейтральной зоны [−0.1, 0.1], что соответствует ранней фазе асимметрии в пользу человечества.

---

## 7. Расписание синхронизации протоколов

### 7.1. Генерация vs. видимость

Протоколы генерируются в **базе D1**, но появляются в **git-репозитории** (`data/protocols/`) с задержкой. Это не баг — синхронизация выполняется отдельным GitHub Actions workflow по расписанию.

### 7.2. Таблица задержек

| Тип протокола | Генерация (D1) | Видимость (git) | Задержка |
|---------------|----------------|-----------------|----------|
| **Interim** (черновик) | Friday 13:45 UTC | **Saturday 08:00 UTC** | ~18 часов |
| **Final** | Monday 13:45 UTC | **Monday 14:00 UTC** | ~15 минут |

### 7.3. Почему задержка?

- **Interim:** генерируется Friday 13:45, синхронизируется Saturday 08:00 — позволяет провести weekend-review перед публикацией.
- **Final:** генерируется Monday 13:45, синхронизируется Monday 14:00 — почти мгновенная публикация.

### 7.4. Sync workflow

Определён в `.github/workflows/sync-protocols.yml`:

    on:
      schedule:
        - cron: "0 14 * * 1"   # Monday 14:00 UTC — синхронизация final
        - cron: "0 8 * * 6"    # Saturday 08:00 UTC — синхронизация interim

Workflow:
1. Забирает последние 2 протокола из D1 API (`/export-weekly?weeks=2`).
2. Пишет в `data/protocols/` в collector-репозитории.
3. Архивирует все протоколы в `human-ai-monitor-archive` репозиторий.
4. Запускает translation workflow (EN→RU/ZH).

### 7.5. Ручная синхронизация

    gh workflow run sync-protocols.yml

Полная документация: `docs/architecture.md` §6 «Protocol Synchronization».

---

## 8. Что сделано (v1.0.1)

| Компонент | Статус |
|-----------|--------|
| Архитектура | Cloudflare Workers + D1 + Workers AI (модульная, 14 модулей) |
| Классификатор | Qwen 3 (открытые веса) на Cloudflare Workers AI |
| Конфигурация | `wrangler.jsonc` с D1 binding, 5 cron batches |
| Инфраструктура | D1 создана (EEUR), 4 таблицы заполнены |
| Код | TypeScript, 20 API endpoints, 41 источник |
| Тесты | **66/66 проходят** (~75% покрытие) |
| Аудит | **19/19 ok, 0 warn, 0 FAIL** (`scripts/repo_audit.py`) |
| CI/CD | GitHub Actions: CI, docs-check, sync, translate |
| Документация | README × 3 (EN/RU/ZH), docs/, research/ × 3 |
| Методология | `docs/methodology.md`, `docs/math_brief.md` |
| Live API | https://human-ai-monitor-collector.human-ai-monitor.workers.dev |
| Автономность | ✅ Первый полностью автономный дневной цикл завершён (2026-09-25) |

### Критические исправления v1.0.1 (25 сентября 2026)

В первоначальном релизе v1.0.0 были обнаружены и исправлены два критических бага:

**1. Русские ключи в gap-computation.ts (commit `13fa1c1`)**

Словари мультипликаторов `SHIFT_MULT` и `DIR_MULT` были объявлены с русскими ключами (`'да'`/`'нет'`/`'рост'`/`'падение'`), тогда как классификатор возвращал английские ключи (`yes`/`no`/`up`/`down`). Это приводило к тому, что lookup возвращал `undefined`, и fallback `?? 1` давал единичный множитель.

**Влияние:** Модель теряла чувствительность к сдвигам порогов и направленным трендам, фактически сводясь к средней relevance по оси.

**Исправление:** Все ключи мультипликаторов переведены на английский, восстановлена полная функциональность математической модели.

**2. Hang Miniflare после тестов (commit `5f5721d`)**

После прохождения всех 66 тестов Miniflare не закрывал ресурсы WebSocket, ZLIB потоки и FileHandle, что вызывало 10-секундный timeout hang (24s общее время вместо ~5s).

**Исправление:** Добавлен `test/global-teardown.ts` с 2-секундным grace period с последующим `process.exit(0)`. Уменьшен `teardownTimeout` с 10s до 3s. Отключены `remoteBindings` в тестах.

**Результат:** Тестовый набор теперь выполняется за **8 секунд** вместо 24 секунд.

---

## 9. Приглашение

Если вы математик, и любая из открытых проблем (формализация RSI, обнаружение фазовых переходов, верификация без оракула, динамика Gap Index) вам интересна — мы приглашаем вас к сотрудничеству.

**Проект открыт. Код открыт. Данные открыты.**

**Вместе — Мы Сила. Дорогу осилит идущий.**

---

**Контакты:**
- GitHub: https://github.com/VQQLK/Human-AI-Monitor
- Issues: https://github.com/VQQLK/Human-AI-Monitor/issues
- API: https://human-ai-monitor-collector.human-ai-monitor.workers.dev
